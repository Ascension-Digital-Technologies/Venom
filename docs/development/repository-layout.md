# Repository layout

```text
src/compiler/commands   CLI-facing commands
src/compiler/core       configuration, process, project, and site foundations
src/compiler/pipeline   compilation, packaging, JavaScript, HTML, CSS, and security
src/compiler/services   runtime and update services
src/generated/runtime   generated runtime assembly and embedded artifacts
src/runtime             native/WASM runtime implementations and probes
src/runtime/templates   auditable JavaScript runtime templates
tools                   canonical build, verification, packaging, and release tools
tests                   static, integration, runtime, package, and browser regressions
examples                flagship protected applications
```
