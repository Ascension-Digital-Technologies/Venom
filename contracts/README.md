# Venom Contracts

This directory contains Venom's authoritative machine-readable contracts and policy inputs.

These files are not C++ source. Generated native interfaces belong under `src/generated/`, while the authoritative machine-readable inputs live under `contracts/`.
