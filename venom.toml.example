# Venom - Secure Web Runtime project configuration
[project]
name = "example-site"
entry = "examples/basic-site"
output = "dist"

[build]
vendor_cache = ".venom/vendor-cache"
vendor_lock = "examples/basic-site/venom.lock"
offline = false
refresh_vendors = false

[runtime]
engine = "quickjs-wasm"
fail_closed = true

[security]
deny_host_js_fallback = true
require_audited_crypto = false

[compatibility]
minimum_chrome = 120
minimum_firefox = 121
minimum_safari = 17
