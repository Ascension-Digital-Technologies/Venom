# Remote vendor cache fixtures

Deterministic JavaScript fixtures used by offline compiler tests. Production builds use the same SHA-256 keyed cache format but download the real HTTPS response when a valid cache entry is absent.
