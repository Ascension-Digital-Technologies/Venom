[project]
entry = "."
output = "dist"
target = "chrome-extension"

[build]
profile = "prod"

[runtime]
engine = "turbojs-wasm"
fail_closed = true

[security]
deny_host_js_fallback = true

[planner]
mode = "auto"
