# Venom + Vite showcase

A minimal Vite application using `@venom-js/vite`. Vite provides the normal TypeScript development pipeline while Venom emits a separate protected QuickJS/WASM distribution.

```bash
npm install
npm run dev
```
