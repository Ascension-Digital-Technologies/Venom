# Venom + Vite showcase

A minimal Vite application using `@venom/vite`. Vite provides the normal TypeScript development pipeline while Venom emits a separate protected TurboJS/WASM distribution.

```bash
npm install
npm run dev
```
